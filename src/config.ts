/**
 * Depd option interface.
 */
export interface opts {
    /**
     * Whether or not to use colors.
     */
    colors?: boolean;

    /**
     * Throw errors or just log a warning.
     */
    error?: boolean;
}

export default opts;